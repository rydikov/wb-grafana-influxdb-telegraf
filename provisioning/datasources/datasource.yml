apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    isDefault: true

    url: http://influxdb:8086

    database: metrics

    user: admin

    jsonData:
      httpMode: GET
      version: InfluxQL

    secureJsonData:
      password: changemeadminpassword